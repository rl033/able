<nav class="navbar navbar-custom navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header navbar-header-custom">
      <!-- <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button> -->
      <%=link_to('TalentSpot', root_path, class: "web-title logo")%>
    </div>
    <div class="nav-section" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if account_signed_in? %>
          <li><%=link_to('Dashboard', dashboard_path(current_account.id), class: "web-title nav-link")%></li>
          <li><%=link_to('Discover', projects_path, class: "web-title nav-link")%></li>
        <% end %>
      </ul>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <div class="user-button">
          <% if account_signed_in? %>
            <div class="dropdown">
              <button class="btn dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                <%= current_account.display_name %>
                <span class="caret"></span>
              </button>
              <ul class="dropdown-menu dropdown-menu-right pull-right" aria-labelledby="dropdownMenu1">
                <li><a href="#">Profile</a></li>
                <li><a href="#">Settings</a></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to "Log out", destroy_account_session_path, method: :delete %></li>
              </ul>
            </div>
          <% else %>
            <%=link_to('Sign in', new_account_session_path, class: 'btn btn-default navbar-btn outline sign-in-btn')%>
            <%=link_to('Sign up', new_account_registration_path, class: 'btn btn-primary navbar-btn outline sign-up-btn')%>
          <% end %>
        </div>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>